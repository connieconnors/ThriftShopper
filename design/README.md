
  # Mood Wheel Options

  This is a code bundle for Mood Wheel Options. The original project is available at https://www.figma.com/design/MGqpRkYnq552j1wB3ifR60/Mood-Wheel-Options.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  